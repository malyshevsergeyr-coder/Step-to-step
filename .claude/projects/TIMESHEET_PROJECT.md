# Проект: Табель учёта рабочего времени (Посещаемость)

## Общая информация

**Назначение:** Автоматический парсинг отчётов из системы контроля доступа Gate (SoftGate) и формирование табеля рабочего времени.

**Таблица:** https://docs.google.com/spreadsheets/d/1yF9iuHcFaTyJ2X6PntO7661HFukmSgQWydhZsukHsC8

**Apps Script проект:** https://script.google.com/u/0/home/projects/1UEYbSqyWw34Tduy5RUfKFkTOAuvLVqRxECDw0pz_g_9JJRoSfJs1U5Qk/edit

**Локальная папка:** `C:\Users\office\Documents\Google Web\Посещаемость\`

---

## Структура таблицы

### Листы:
1. **Sheet1** - сырые данные из Excel (скрыт)
2. **gate_events** - распарсенные события (вход/выход)
3. **табель** - сводный табель по сотрудникам и дням

### Колонки gate_events:
| Колонка | Название | Формат |
|---------|----------|--------|
| A | ФИО | текст |
| B | Ключ | текст |
| C | Дата | dd.MM.yyyy |
| D | Время входа | HH:mm |
| E | Время выхода | HH:mm |
| F | Минут | число |
| G | Часов | H:mm |
| H | Тип | полный/вход/выход |

### Структура листа "табель":
- **Строки 1-6:** Шапка (заголовок, период, статистика, дата обновления, место для кнопки)
- **Строка 7:** Заголовки данных (ФИО | Дата1 | Дата2 | ... | ИТОГО)
- **Строки 8+:** Данные сотрудников

---

## Процесс работы

### Алгоритм обработки:
1. Пользователь выгружает отчёт из Gate в формате Excel (.xlsx)
2. Загружает файл в папку "Отчёты Gate" на Google Диске
3. Нажимает кнопку или запускает функцию `обработатьGateСКнопки`
4. Система:
   - Находит новые файлы в папке
   - Конвертирует Excel в Google Sheets (временно)
   - Импортирует данные в Sheet1
   - Парсит события (parseGateData)
   - Строит сводный табель (buildTimesheet)
   - Форматирует таблицы (formatTimesheetTables)
   - Помечает файл как обработанный (добавляет префикс `[Обработан]`)
5. Показывает результат во всплывающем окне

### Ключевые функции:
- `processNewGateFiles()` - главная функция обработки
- `обработатьGateСКнопки()` - функция для кнопки с UI
- `parseGateData()` - парсинг сырых данных в события
- `buildTimesheet()` - построение сводного табеля
- `formatTimesheetTables()` - профессиональное оформление

---

## Возникшие проблемы и решения

### 1. Функция не появлялась в Apps Script
**Проблема:** После clasp push функции не были видны в проекте.
**Причина:** clasp был подключен к другому проекту (scriptId).
**Решение:** Создали отдельную папку `Посещаемость` с правильным `.clasp.json` и scriptId проекта табеля.

### 2. Конфликт констант (SyntaxError: Identifier already declared)
**Проблема:** Ошибка при запуске - дублирование констант.
**Причина:** clasp отправил оба файла Code.js и Код.js в один проект.
**Решение:** Удалили лишние файлы из Apps Script вручную, создали отдельную структуру папок.

### 3. Парсер возвращал 0 событий
**Проблема:** События не парсились из нового формата отчёта Gate.
**Причина:** Формат отчёта изменился - колонки сдвинулись (было 7 колонок, стало 12).
**Решение:** Переписали парсер с гибким поиском дат/времени по паттернам, а не по фиксированным индексам.

### 4. Даты отображались как "Wed Feb 04 2026..."
**Проблема:** Вместо "04.02.2026" показывалась полная строка Date.
**Причина:** Google Sheets возвращает Date объекты, а не строки.
**Решение:** Добавили функцию `normalizeDateStr_()` для преобразования Date в DD.MM.YYYY.

### 5. Время отображалось неправильно (3:25 вместо 16:55)
**Проблема:** Время сдвигалось из-за часовых поясов.
**Причина:** Excel хранит время как Date с базовой датой 1899, при конвертации терялся часовой пояс.
**Решение:** Использовали `getDisplayValues()` вместо `getValues()` для чтения данных как текст.

### 6. Ошибка "Нельзя объединять закрепленные и незакрепленные столбцы"
**Проблема:** Ошибка при создании шапки табеля.
**Причина:** Пытались объединить ячейки через границу закреплённого столбца.
**Решение:** Убрали merge() для заголовка.

### 7. Дата отображалась как "0:00:00"
**Проблема:** В заголовке табеля вместо даты показывалось время.
**Причина:** Ячейка была отформатирована как "Время", показывалась только временная часть.
**Решение:** Добавили явное форматирование колонок: дата как `dd.MM.yyyy`, время как `HH:mm`.

---

## TODO на понедельник

### Табель (текущие баги):
1. [ ] Проверить форматирование дат и времени после исправлений
2. [ ] Добавить кнопку-рисунок на лист "табель"
3. [ ] Протестировать полный цикл обработки
4. [ ] Получить чистовой шаблон табеля от начальницы

### Штатная расстановка (новый функционал):
5. [ ] Создать папку "Штатная расстановка" на Google Диске
6. [ ] Добавить лист "штатная" в таблицу табеля
7. [ ] Написать функцию обработки файлов штатной расстановки
8. [ ] Реализовать сверку: Gate vs Штатная
9. [ ] Добавить подсветку расхождений (уволенные, новые без проходов)

### Организационные вопросы:
10. [ ] Обсудить с директором/ОК включение в цепочку кадровых документов
11. [ ] Распечатать и разместить чек-листы приёма/увольнения

---

## Технические детали

### Константы:
```javascript
const TIMESHEET_SPREADSHEET_ID = '1yF9iuHcFaTyJ2X6PntO7661HFukmSgQWydhZsukHsC8';
const SHEET_GATE_RAW = 'Sheet1';
const SHEET_GATE_EVENTS = 'gate_events';
const SHEET_TABEL = 'табель';
const GATE_FOLDER_NAME = 'Отчёты Gate';
const GATE_PROCESSED_PREFIX = '[Обработан] ';
const TABEL_DATA_START_ROW = 7;
```

### Требуемые API:
- Google Sheets API
- Google Drive API v2 (для конвертации Excel)

### Развёртывание:
```bash
cd "C:\Users\office\Documents\Google Web\Посещаемость"
clasp push --force
```

---

---

## Интеграция со штатной расстановкой (ПЛАН)

### Цель
Сверка данных Gate (факт посещений) со штатной расстановкой из 1С (актуальный состав) для формирования полного табеля, включающего уволенных и новых сотрудников.

### Логика сверки

| Ситуация | Gate | Штатная | Действие |
|----------|------|---------|----------|
| Работающий сотрудник | ✅ | ✅ | Норма |
| Уволенный в течение месяца | ✅ | ❌ | Оставить в табеле (отработал часть месяца) |
| Новый без проходов | ❌ | ✅ | Проверить: выдан ли пропуск? |
| Неизвестный | ✅ | ❌ | Проверить: кто это? |

### Структура папок на Google Диске

```
📁 Отчёты Gate/              — Excel-файлы из SoftGate
📁 Штатная расстановка/      — Excel-файлы из 1С
```

### Новые листы в таблице

```
штатная     — импортированные данные из 1С
сверка      — результат сравнения Gate и Штатной (опционально)
```

### Источник данных из 1С

**Отчёт:** Штатная расстановка
**Фильтр:** Подразделение = "3. Стекольное производство"
**Сохранённые настройки:** "Штатная - Стекольное производство"
**Формат выгрузки:** Excel (.xlsx)

---

## Чек-листы кадровых операций

### Приём нового сотрудника

```
□ Получить от ОК документ о приёме (приказ/заявление)
□ Узнать дату выхода на работу
□ Внести сотрудника в базу Gate (SoftGate)
□ Выдать/активировать пропуск
□ Проверить тестовый проход через турникет
□ Убедиться, что сотрудник отображается в отчётах Gate
```

### Увольнение сотрудника

```
□ Получить от ОК документ об увольнении
□ Узнать последний рабочий день
□ В последний день: забрать пропуск
□ Деактивировать пропуск в базе Gate
□ Проверить, что в табеле отражены все дни до увольнения
```

### Ежемесячное формирование табеля

```
□ Выгрузить отчёт из Gate за месяц
□ Загрузить в папку "Отчёты Gate"
□ Выгрузить штатную расстановку из 1С (фильтр: стекольное производство)
□ Загрузить в папку "Штатная расстановка"
□ Нажать кнопку обработки
□ Проверить сверку: все ли сотрудники на месте?
□ При расхождениях — выяснить причину
□ Сформировать итоговый табель
```

---

## Организационные задачи

### Обсудить с директором/ОК:

**Предложение:** Включить в цепочку документооборота по кадрам.

> "При оформлении приёма/увольнения — копия приказа мне для своевременного обновления системы контроля доступа (Gate). Это необходимо, чтобы:
> - Новый сотрудник мог пройти на работу с первого дня
> - Пропуск уволенного был деактивирован вовремя
> - Табель учёта рабочего времени формировался корректно"

---

**Последнее обновление:** 06.02.2026
**Версия:** 1.2
